THIS IS THE CHECKLIST FOR THINGS TO GET DONE ON THE PROJECT
General:
- need creating account to also put you in the login state
- figure out getStarted error if a user already has an account in the database
1. Configure listings
    - make the supabase table called listings with all attributes(done)
    - create the ItemCard component that creates the item card with the table information(done)
    - map the ItemCards into the marketplace(done)
    - create the post listing component so that it inserts information into the table
    - import the post listing into the marketplace
    - create the Item component for when a user clicks on the item,
        (should be like a modal with a blur backdrop, gonna make the figma design)
    - need to figure out image upload, accessing camera for post listing
2. User and Profile System

3. Messaging system
